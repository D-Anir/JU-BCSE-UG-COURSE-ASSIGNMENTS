import java.util.*;
import java.util.stream.Collectors;
import javafx.util.Pair;

public class Test {
    public static void main(String args[]) {
        List<Integer> array1 = Arrays.asList(2, 3, 4, 5);
        List<Integer> array2 = Arrays.asList(6, 7, 8);

        // List<Integer> answer = array1.stream().filter(d1 -> d1 % 2 ==
        // 1).collect(Collectors.toList());
        // System.out.println(answer);

        ArrayList<Pair<Integer, Integer>> answer = new ArrayList<Pair<Integer, Integer>>();
    }
}